services:
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: 69-s1-admin
    environment:
      - PGADMIN_DEFAULT_EMAIL=kanjanaphorn-k@rmutp.ac.th
      - PGADMIN_DEFAULT_PASSWORD=satang0706
    ports:
      - "8081:80"
    volumes:
      - ./data/pgadmin:/var/lib/pgadmin
